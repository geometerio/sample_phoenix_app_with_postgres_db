#!/bin/bash

docker build \
  --build-arg DOCKER_BUILDER_ELIXIR_IMAGE=${DOCKER_BUILDER_ELIXIR_IMAGE} \
  --build-arg DOCKER_APP_RUNNER_IMAGE=${DOCKER_APP_RUNNER_IMAGE} \
  --build-arg APP_NAME=${APP_NAME} \
  --tag ${DOCKER_IMAGE_NAME} .
